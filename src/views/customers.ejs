<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
    <meta charset="UTF-8">
    <title>العملاء - <%= company.companyName %></title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.css">
    <link rel="stylesheet" href="/css/sidebar.css">
    <link rel="stylesheet" href="/css/customers.css">
</head>
<body>
    <div class="app-layout">
        <%- include('partials/sidebar', { user: user, activePage: 'customers' }) %>

        <main class="main-content-panel">
            <header class="panel-header">
                <h4 class="flex-grow-1 mb-0">قائمة العملاء</h4>
                <div class="search-container">
                    <div class="input-group">
                        <input type="search" id="customerSearchInput" class="form-control" placeholder="ابحث بالاسم أو الرقم...">
                    </div>
                </div>
            </header>
            <div class="p-4 overflow-auto">
                <div id="customers-loading" class="text-center mt-5">
                    <div class="spinner-border text-primary" style="width: 3rem; height: 3rem;" role="status">
                        <span class="visually-hidden">Loading...</span>
                    </div>
                </div>
                <div id="customers-content" class="d-none">
                    <table class="table table-hover align-middle">
                        <thead>
                            <tr>
                                <th>العميل</th>
                                <th>آخر تواصل</th>
                                <th>إجمالي الرسائل</th>
                                <th>حالة المحادثة</th>
                            </tr>
                        </thead>
                        <tbody id="customersTableBody">
                            </tbody>
                    </table>
                </div>
            </div>
        </main>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
    <script src="/js/customers.js"></script>
</body>
</html>