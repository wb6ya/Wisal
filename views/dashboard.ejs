<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
    <meta charset="UTF-8">
    <title>لوحة التحكم</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.css">
    <link rel="stylesheet" href="/css/dashboard.css">
</head>
<body data-cloud-name="<%= cloudName %>">
    <div class="main-app-container">
        <h1 class="mb-4">لوحة تحكم واتساب - <%= company.companyName %></h1>
        
        <div class="row g-0 chat-container border rounded bg-white">
            <div class="col-lg-4 col-md-5 conversations-list-container p-0">
                <div class="conv-header p-3">
                    <h5 class="mb-0">المحادثات</h5>
                </div>
                <div class="list-group list-group-flush" id="conv-list">
                    </div>
            </div>

            <div class="col-lg-8 col-md-7 chat-window-container p-0">
                <div class="chat-header p-3">
                    <div id="chat-header-content" class="d-flex align-items-center" style="visibility: hidden;">
                        <div id="chat-header-avatar" class="avatar"></div>
                        <span id="chat-header-name" class="fw-bold ms-3"></span>
                    </div>
                </div>
                <div class="messages-area">
                    <div class="d-flex h-100 justify-content-center align-items-center">
                        <h4 class="text-muted">اختر محادثة من القائمة</h4>
                    </div>
                </div>
                <div class="reply-area p-3 bg-light">
                    <form id="replyForm" class="d-none">
                        <div class="input-group">
                            <input type="file" id="imageUpload" accept="image/*,application/pdf,.doc,.docx" style="display: none;">
                            <button class="btn btn-secondary" type="button" id="attachBtn"><i class="bi bi-paperclip"></i></button>
                            <input type="text" id="replyMessage" class="form-control" placeholder="اكتب رسالة..." autocomplete="off">
                            <button type="submit" class="btn btn-primary"><i class="bi bi-send-fill"></i></button>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>

<script src="/socket.io/socket.io.js"></script>
<script src="/js/dashboard.js"></script>
</body>
</html>